{
  "name": "agency",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7"
  },
  "scripts": {
    "build": "bun run --filter='*' build",
    "test": "bun test",
    "typecheck": "bun run --filter='*' typecheck"
  }
}
