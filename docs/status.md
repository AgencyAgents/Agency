# Status

Built means implemented and tested. Wired means a production caller
invokes it. Planned means designed in docs/orchestration.md but not
implemented. Unwired rows name what stands in for the mechanism today.

| Item | Built | Wired | Planned |
|---|---|---|---|
| TeamStore and appendMessage broadcast | Yes, with tests | Partial: `ensureTeamForLeadSession` pins one team per lead session; board tools (`board_read`, `board_claim`, `board_status`, `task_file`) plus `owners_read` are per-agent gated tools and served over /rpc with `team_status` | Team lifecycle with team IDs per turn |
| Retired early-draft team names | No: the names have zero source hits | No | Nothing: the vocabulary is retired in favor of team |
| Per-role prompts and resolveFamilyPrompt | Yes, tables plus selector with tests | Yes: dispatch and dispatch_compare build the child prompt through `buildTeamPrompt` with `resolveFamilyPrompt` plus file-body extend or replace | Per-model prompt length |
| File-based agents (`.agency/agents` plus `resolveFileRoster`) | Yes: `parseAgentFile` validates `role`, `provider`, `model`, `effort`, `tools`, `permissions`, `pathScope`, `replace` with the body as the role prompt, with tests | Yes: `initTeamFromConfig` seeds `DEFAULT_ROSTER` on first run, `agents_list` serves file roles, `agents_upsert` writes the file and refreshes the registry, `importClaudePlugin` writes imports as files, `collectPluginAgents` registers plugin agents; bodies extend the built-in prompt via `resolveRolePrompt` unless `replace` is true | Prompt layering per model |
| Team protocol text and appendTeamProtocol | Yes, baked into role tables | Yes: layered into every child prompt L4 through `buildTeamPrompt`; `PLAYBOOK` is the shared byte-identical L2 block | None |
| planDispatchBatch and DispatchStateStore | Yes, with tests | Yes: daemon dispatch plans every batch through `planDispatchBatch` (inline skips stay as race guards); `planBoardBatch` reuses it for board items at depth 0 | Board as the delegation ledger |
| Category routing and runWithCategoryFallback | Yes, with tests | Yes: `fallbackChainFor` selects the escalation-ladder chain per item (`resolveLadderChain`); `checkDelegation` routes lateral work by capability over registry facts | Capability routing behind the delegation check |
| Cheap routing (selectModel, withCheapFallback) | Yes, with tests | Yes: honored inside runTurn via taskKind and cheapModel; the child-turn assembly carries them for background-eligible turns and title generation routes through them; `buildDigest` builds the tail digest deterministically | None |
| Session and config surface (session_list, session_create, session_export, session_rename, models_list, config_get, config_set, permissions_list, todo_read, todo_write, cost_report, undo_run, prompt_inspect) | Yes, with tests | Yes: served over /rpc and the SDK surface; session todos hand off via `todosToBoard` and `boardToTodos`; `undo_run` rolls back team integration checkpoints after the session stack; `cost_report` carries per-session tokens with cache-hit rate, per-agent plus per-task attribution summing to the run total, spend versus daily and monthly caps, and the team MCP process cap; `session_create` takes a first-class budget, `session_send` falls back to it, and `cost_meter` plus `cost_report` events ride the event stream | None |
| `session_send` with daemon-owned sessions | Yes: `session_send` owns store, history, tokenizer, compaction, appends, and usage, with tests | Yes: `runSessionTurn` is a thin consumer sending text and streaming events; `run_turn` keeps its caller-owned shape | The two-crossing boundary protocol |
| Team isolation (per-team sessions, worktrees, inboxes) | Yes: children keyed by parent session, handle, and batch with namespaced worktrees and fail-closed creation | Yes: dispatch and dispatch_compare isolate sessions, worktrees, inboxes, and cost per parent; SessionScope teamId marks child scopes; claimed items layer `withItemScope` over the agent gate | Disjoint path scopes plus lead-owned integration |
| Resumable events across reconnects | Yes: EventRing issues monotonic ids with retry frames, Last-Event-ID gap-tails, the state frame covers over-buffer reconnects | Yes: /events serves ids, retry, backlog, and state on every connect | None |
| SDK (connect, createAgencyClient, createSurfaceClient) | Yes: generated.ts emits every RPC method plus GatewayEvent types from /doc | Yes: the SDK-only script drives create, send, stream, approve, cost report, export | None |
| Headless permission modes (`PermissionMode`) and bounded approvals | Yes, with tests | Yes: --permission-mode selects ask, allow-edits, or deny; `createPending` fails closed with a typed reason | Headless runs that never stall on an ask |
| At-handle routing (`parseHandles`) | Yes | Yes: lone mentions rewrite provider, model, and effort inline, including through `session_send` | Selection model with escalation never a user choice |
| Compaction (0.9 trigger, 0.7 target, 20K cap) | Yes | Yes daemon-side via `session_send` (proactive plus reactive `onContextOverflow`); `run_turn` keeps `needsCompaction` for caller-owned turns; team agent windows compact item-aware with `item_state` surviving `planCompaction` | Cache-event accounting |
| OAuth (PKCE plus device flow) | Yes | Blocked: all shipped client IDs are placeholders rejected by assertClientIdConfigured | Same flows once the user registers an app |
| Scheduler multi-key rotation (scheduleWithKeys) | Yes, with tests | Yes: dispatch and compare run child turns through the shared per-provider `schedulerFor` under the team cap (`createTeamLimiter`, 5 slots); rotation still covers key failover | Model ladder escalation across families |
| Progress checklist store | Yes, with tests | Yes: `boardToPlanFile` projects tasks to Todos and reviews to Final, `planFileToBoard` picks up human edits, `ProgressStore` carries per-item agent attribution, state lives at `.agency/progress.json` with `.omo/perseverance.json` legacy import | None |
| Progress checklist store | Yes, with tests | Yes: `boardToPlanFile` projects tasks to Todos and reviews to Final, `planFileToBoard` picks up human edits, `ProgressStore` carries per-item agent attribution, state lives at `.agency/progress.json` with `.omo/perseverance.json` legacy import | None |
| Session projector (SessionProjector, projectSessionView) | Yes, with tests | Yes: session_show and the SSE state frame both render through projectSessionView | None |
| dispatch_compare as compare team mode | Yes | Yes: one shared board item that N agents `compareClaim`, with the verdict recorded via `recordCompareVerdict` | None |
| Team depth rule | None: the team path is flat by construction | None: depth-1 nesting lives only on the `spawn` tool | None |
| Eval harness (pass-rate and cost per task per roster) | Yes: `scoreReport` over `loadCassettes` with tests | Yes: run.ts replays cassettes through `replayTask` with zero provider calls and writes the baseline; reviewer-first 1.0 pass-rate at $0.0250 per completed task with 0 conflicts; ladder $0.0255 beats fixed $0.0600 per completed task; scoring prices cache writes at the write premium through `turnCostUsd`, the same path as `cost_report`, with cache-hit rate as a report column | None |
| Lateral delegation (`delegate` plus `checkDelegation` plus `materializeDelegate`) | Yes, with tests | Yes: `delegate` tool files a durable addressed board item; the check delegates only on missing skill or tool, busy parallel work, or a materially cheaper specialist; `activity_graph` rebuilds the delegation DAG from board state alone | None |
| Typed inbox (`inbox_send`) plus pull channel (`channel_read`) | Yes: `InboxStore` with per-agent budgets and lead-only broadcast, `ChannelStore` with cursor reads, with tests | Yes: both registered per scope and served over /rpc; child drains deliver inbox plus `buildDigest` at the tail | None |
| Choice log (`ChoiceLog` plus `decisions`) | Yes: propose, accept, and conflict routes to lead, with tests | Yes: `decisions` tool serves read, propose, and accept; `decisions_read` serves the entries over /rpc; every child prompt carries the digest | None |
| Layered team prompt (L0 to L7 plus `PLAYBOOK` plus `LAYER_BUDGETS`) | Yes: `buildTeamPrompt` with `resolveRolePrompt`, per-layer budgets, and `auditPromptToolOverlap`, with tests | Yes: dispatch and dispatch_compare compose through it; budgets enforced in CI | Per-model prompt length |
| Team digest (`buildDigest` plus `DIGEST_LINE_BUDGET`) | Yes, with tests | Yes: delivered at the tail of every child drain from board events with cursor tracking per child | None |
| Inspection (`agent_inspect` over spans, cassettes, and history) | Yes: `spansToInspectable`, `inspectTimeline`, `inspectStep`, `inspectReasoning`, `canInspect`, `redactInspectText`, with tests | Yes: lead-only tool over `loadTraceSpansSync` plus board claim sets, charged per call; same three granularities served over /rpc | None |
| Team report (`report_get` plus `buildTeamReport`) | Yes, with tests | Yes: structured report with no transcripts crosses back to the lead and is served over /rpc; the board listener emits it automatically on completion and halts on the no-progress detector; cost carries per-agent totals with the measured cache-hit rate, and a `cost_report` event with the same numbers follows it | None |
| Lead demotion (`isTeamLive` plus `demoteLeadTools` plus `LEAD_WITHHELD_TOOLS`) | Yes, with tests | Yes: lead turns and lead scopes drop `write`, `edit`, and `bash` while board items are open | None |
| Escalation trigger (`decideEscalation` plus `team_open`) | Yes, with tests | Yes: `team_open` opens behind approval on 3+ disjoint items, a long plan, or explicit request, with a one-line `formatTeamAnnouncement` before the first spawn; every dispatch announces its `estimateDispatchCost` range on `dispatch_start` and refuses past daily or monthly caps before spawning | None |
| Reviewer-first composition (`defaultTeamComposition`) | Yes, with tests | Yes: one writer plus reviewers capped at 5; `DEFAULT_ROSTER` spreads providers and `resolveRosterProviders` remaps to credentialed ones at startup | None |
| Model ladder (`resolveLadderChain` plus `climbLadder` plus `retryTarget`) | Yes, with tests | Yes: items start cheap and climb one rung per bounce or decline with failure notes; retry moves to a different family one rung up | None |
| Disagreement protocol (`fileObjection` plus `recordTiebreak`) | Yes, with tests | Yes: evidence-bearing objections, one rebuttal round, then a third-provider `selectTiebreaker` with the verdict recorded on the board | None |
| LSP review gate (`gateReadyForReview`) | Yes, with tests | Yes: new error diagnostics in the item scope refuse `ready_for_review`; reviewer findings land via `appendReviewFindings` | None |
| Integration checkpoint (`recordIntegrationCheckpoint` plus `restoreIntegrationCheckpoint`) | Yes, with tests | Yes: pre-merge capture recorded at `team_open`; `undo_run` restores the workspace at team scope | None |
| Team MCP policy (`TeamMcpPool`) | Yes, with tests | Yes: read-only or stateless servers start once per team; the process cap surfaces in `cost_report` | None |
| Run interruption (`team_stop` plus `agent_stop`) | Yes, with tests | Yes: `agent_stop` aborts one handle's turns and idles its sessions | None |
